[package]
name = "yang-db"
version = "0.1.0"
edition = "2024"

description = "个人使用数据库操作"
license = "MIT OR Apache-2.0"

#排除文件
exclude = [
    "INSTALL.md",
]

[dependencies]
deadpool-redis = "0.23.0"
redis = "1.1.0"
sqlx = "0.8.6"
tokio = "1.51.0"
